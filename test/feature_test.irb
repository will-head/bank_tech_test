require 'date'
require 'money'
require_relative 'lib/transaction'
require_relative 'lib/account'
require_relative 'lib/statement'

account = Account.new

account.add (Transaction.new(Date.new(2012, 1, 10), 1000 * 100))

account.add (Transaction.new(Date.new(2012, 1, 13), 2000 * 100))

account.add (Transaction.new(Date.new(2012, 1, 14), -500 * 100))

puts "\n\n" + Statement.show(account) + "\n"